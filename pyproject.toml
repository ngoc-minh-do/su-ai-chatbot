[project]
name = "su-ai-chatbot"
version = "0.1.0"
description = ""
authors = [{ name = "ngoc", email = "ngochust56@gmail.com" }]
readme = "README.md"
requires-python = ">=3.13"

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
gradio = "^5.31.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
